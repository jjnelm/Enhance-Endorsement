export interface FormData {
  bankName: string;
  accountNumber: string;
  referenceNumber: string;
  amount: string;
  traceNumber: string;
  firstName: string;
  lastName: string;
  email: string;
  notes: string;
}